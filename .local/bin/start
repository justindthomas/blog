#!/bin/bash

service postgresql start

cd /srv/blog && \
    .local/bin/blog -e prod && \
    pkill blog && \
    su -c '/usr/bin/psql blog < /srv/blog/sql/create_nav_view.sql' postgres && \
    .local/bin/blog -e prod
