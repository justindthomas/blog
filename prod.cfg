postgresql-simple
{
  host = "db.service.dc1.consul"
  port = 5432
  db = "blog"
  user = "blog"
  sslcert = "credentials/blog-client.db.cert.pem"
  sslkey = "credentials/blog.db.key.pem"
}
sass
{
  compileMode = "Production"
  style = "nested"
  sourcemap = on
  verbose = on
}